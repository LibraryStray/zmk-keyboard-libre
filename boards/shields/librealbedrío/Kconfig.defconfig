if SHIELD_LIBREALBEDRÍO_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "librealbedrío"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_LIBREALBEDRÍO_LEFT || SHIELD_LIBREALBEDRÍO_RIGHT

config ZMK_SPLIT
    default y

endif
